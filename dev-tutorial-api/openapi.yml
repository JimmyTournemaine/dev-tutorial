---
openapi: 3.0.0
info:
  title: Dev' Tutorial REST API
  version: "1.0"
  description: |
    The main resource of the REST API is the `Tutorial`.
    You will load tutorials, get its slides content and start/stop its environement container.
    Once you container is started, you should [attach a websocket](../asyncapi/index.html) to send commands.
servers:
  - url: https://localhost:3000/api/
# rules and schemas are generated from JSDoc
