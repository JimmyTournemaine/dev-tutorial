---
- name: Destroy
  hosts: localhost
  connection: local
  gather_facts: false
  no_log: "{{ molecule_no_log }}"
  tasks:
    - name: Destroy images
      docker_image:
        name: dev-tutorial-api-prod-molecule
        state: absent

- import_playbook: ../../../run-api/molecule/default/cleanup.yml
- import_playbook: ../../../build-api/molecule/default/cleanup.yml
